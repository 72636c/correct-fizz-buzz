---
kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: build + test + exec
    image: haskell:8.6.3
    commands:
      - stack test --fast
      - stack exec correct-fizz-buzz-exe
    when:
      event:
        exclude:
          - pull_request
          - tag

---
kind: signature
hmac: b56a08fb5078e39dc740ab912c5ac98de9869049bde0a19b3b677bfe3eb264c6

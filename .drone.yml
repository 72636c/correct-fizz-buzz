kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: build + test + exec
    image: haskell:8.6.3
    commands:
      - stack test
      - stack exec correct-fizz-buzz-exe
    when:
      event:
        exclude:
          - pull_request
          - tag
